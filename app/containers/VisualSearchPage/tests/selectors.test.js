// import { selectVisualSearchPageDomain } from '../selectors';

describe('selectVisualSearchPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
