/*
 * VisualSearchPage Messages
 *
 * This contains all the text for the VisualSearchPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.VisualSearchPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the VisualSearchPage container!',
  },
});
